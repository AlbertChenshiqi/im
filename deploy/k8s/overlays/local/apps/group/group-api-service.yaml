apiVersion: v1
kind: Service
metadata:
  name: group-api
  labels:
    app: group-api
    app.kubernetes.io/name: group-api
    app.kubernetes.io/component: group
spec:
  selector:
    app: group-api
  type: NodePort
  ports:
  - port: 10300
    targetPort: 10300
    nodePort: 30300
    name: http
