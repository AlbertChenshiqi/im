apiVersion: v1
kind: Service
metadata:
  name: cron
  labels:
    app: cron
    app.kubernetes.io/name: cron
    app.kubernetes.io/component: cron
spec:
  selector:
    app: cron
  type: NodePort
  ports:
  - port: 10800
    targetPort: 10800
    nodePort: 30800
    name: http
