apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-api
  labels:
    app: user-api
    app.kubernetes.io/name: user-api
    app.kubernetes.io/part-of: im
    app.kubernetes.io/component: user
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-api
  template:
    metadata:
      labels:
        app: user-api
        app.kubernetes.io/name: user-api
        app.kubernetes.io/component: user
    spec:
      containers:
      - name: user-api
        image: im/user/user-api:dev
        imagePullPolicy: IfNotPresent
        command: ["/usr/local/bin/user-api"]
        args: ["-f", "/etc/im/user/user-api.yaml"]
        ports:
        - containerPort: 10100
          name: http
        readinessProbe:
          tcpSocket:
            port: 10100
          initialDelaySeconds: 5
          periodSeconds: 10
        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            memory: 512Mi
        volumeMounts:
        - name: config
          mountPath: /etc/im/user
          readOnly: true
      volumes:
      - name: config
        configMap:
          name: user-api-config
