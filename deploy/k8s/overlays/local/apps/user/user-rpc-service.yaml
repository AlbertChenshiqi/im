apiVersion: v1
kind: Service
metadata:
  name: user-rpc
  labels:
    app: user-rpc
    app.kubernetes.io/name: user-rpc
    app.kubernetes.io/component: user
spec:
  selector:
    app: user-rpc
  ports:
  - port: 20100
    targetPort: 20100
    name: http
