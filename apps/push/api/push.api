syntax = "v1"

info (
	title: "Push API"
)

type StatusResp {
	Status string `json:"status"`
}

@server (
	prefix: /push/v1
	jwt:    Auth
)
service push-api {
	@handler setOnline
	post /online returns (StatusResp)
}

